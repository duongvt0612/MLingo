# Translation Engine

TranslationProvider is capability-scoped and supports OpenAI-compatible remote endpoints and built-in MLX. Sentence fragments are assembled before translation, and only complete translations reach overlay or TTS.
