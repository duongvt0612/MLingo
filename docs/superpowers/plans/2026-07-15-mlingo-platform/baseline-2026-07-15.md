# MLingo Platform Migration Baseline

**Captured:** 2026-07-15

**Branch:** `dev`

**Runtime scope:** Existing MVP before capability/event migration

## Repository baseline

- Swift tools 6.3, macOS 14+, Apple Silicon target.
- Current runtime is centered on `SubtitlePipeline` and supports system-audio capture, local MLX Whisper, OpenAI Responses translation, floating subtitle overlay, settings persistence, and performance diagnostics.
- Existing package resolution already includes MLX Swift LM 3.31.4, swift-huggingface 0.9.0, and swift-transformers 1.3.3 transitively through MLX Audio. Direct product linkage is deferred to Milestone 07.
- The original eight MVP plan files under `docs/plans/` are historical implementation records. The active roadmap is this 17-milestone platform plan.

## Verification evidence

| Check | Result |
|---|---|
| `rtk swift test --no-parallel` | PASS: 175 Swift Testing tests, 0 failures |
| `rtk swift build -c release` | PASS |
| Default external/live tests | Correctly skipped unless explicit environment flags are enabled |

## Classified warning

The Release build reports one upstream SwiftPM resource warning from the checked-out `mlx-audio-swift` dependency:

```text
mlx-audio-swift/Sources/MLXAudioVAD/Models/SileroVAD/README.md is unhandled
```

This file is outside MLingo source ownership, does not fail the build, and is not suppressed or patched locally. Re-evaluate it when the dependency version changes.

## Local RC scope

The repository contains the existing local RC build path and related tests. Hardware/TCC behavior, live OpenAI calls, model downloads, signing, and notarization remain opt-in acceptance checks and are not implied by the deterministic default test result.
